cd ${PKG_ROOT}
go run ${WORK}/main.go
cd ${WORK}
cmp stdout want.txt

-- main.go --
package main

import . "github.com/efd6/dispear"

func main() {
	DESCRIPTION("foreach")

	FOREACH("src",
		APPEND("dst", "{{{_ingest._value}}}"),
	)

	Generate()
}
-- want.txt --
---
description: foreach
processors:
  - foreach:
      tag: foreach_of_src_6a31db78
      field: src
      processor:
        append:
          tag: append_dst_8878c5fb
          field: dst
          value: '{{{_ingest._value}}}'
